---
name: Basic-linux
on:
  workflow_dispatch: null
jobs:
   runs-on: ubuntu-latest
    steps:
    -uses: actions/checkout@v3
    -name:
      run: |
        ls
        ls -la
        date
        cat /etc/os-release  
